name: 01 Basics First Workflow

on:
  pull_request:
    types:
      - closed
jobs:
  print-hello-world:
    runs-on: ubuntu-latest
    steps:
      - name: Print hello world
        run: echo "hello world"

