name: 01 Basics First Workflow
run-name: Khaled abdalbasit
on:
   workflow_dispatch:

jobs:
  print-hello-world:
    runs-on: ubuntu-latest
    steps:
      - name: Print context data
        run: echo ${{tojson(github)}}

 
