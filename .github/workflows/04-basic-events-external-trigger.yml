name: 01 Basics First Workflow

on:
  on:
    schedule:
      - cron: "*/5 * * * *"

jobs:
  print-hello-world:
    runs-on: ubuntu-latest
    steps:
      - name: Print hello world
        run: echo "Hello world"

